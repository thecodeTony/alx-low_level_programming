#include "main.h"

/**
 * _abs - computes absolute value of an integer
 * @t: the integer to b computed
 * Return: The absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
